n = input()

alpha = []
num = []

for a in n:
    if a.isalpha():
        alpha.append(a)
    elif a.isdigit():
        num.append(a)

alpha.sort()
num.sort()

c = "".join(alpha) + "".join(num)
print(c)

//문자열 합치기 -> join
