//큰 수의 법칙


a = list(map(int, input().split()))

b = list(map(int, input().split()))

c = sorted(b, reverse=True)

setup = a[2] + 1

k = a[1] // setup
j = a[1] % setup

g = (c[0]*a[2]+c[1]) * k + c[0]*j

print(g)
